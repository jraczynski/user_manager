<% provide(:title, 'Nowy') %>
<h1>Nowy użytkownik</h1>

<div class="row">
  <div class="span6 offset3">
    <%= form_for(@user) do |f| %>
      <%= render 'fields', f: f %>
      </br>
      <%= f.submit "Dodaj użytkownika", class: "btn btn-large btn-primary" %>
    <% end %>
  </div>
</div>